fastapi>=0.110
uvicorn[standard]>=0.29
sqlalchemy>=2.0
pandas>=2.0
xgboost>=2.0
scikit-learn>=1.3
python-multipart>=0.0.9
requests>=2.31
aiofiles>=23.0
python-dotenv>=1.0
pydantic>=2.6

# --- production resilience (Phase 4) ---
slowapi>=0.1.9      # per-IP rate limiting
cachetools>=5.3     # in-process TTL caches
# asyncpg>=0.29     # uncomment for async PostgreSQL connection pooling

# --- restaurant mode (Phase 5) ---
pytesseract>=0.3.10 # OCR bindings (needs the Tesseract engine installed)
Pillow>=10.0        # image decoding for OCR
rapidfuzz>=3.0      # fuzzy dish -> food matching

# --- reinforcement learning (Phase 6) ---
numpy>=1.24         # context vectors + bandit linear algebra

# --- frontend (Phase 8, Streamlit) ---
streamlit>=1.30     # dashboard UI
plotly>=5.18        # weight-trend charts
# pandas already listed above (used for charts too)

# --- testing ---
pytest>=8.0
httpx>=0.27
