# Screenshots

Drop product screenshots here so they render in the main README, then uncomment
the `PRODUCT SCREENSHOTS` block in [`../../README.md`](../../README.md).

Suggested captures (from the running Streamlit app, `make ui`):

| file | page |
|---|---|
| `dashboard.png` | Dashboard — calorie hero + macro progress + today's meals |
| `optimize.png`  | Optimize — ML food recommendations with "Log" |
| `chat.png`      | Chat — meal plan with "Log this meal" |
| `body.png`      | Body & TDEE — weight-trend chart |

Tip: 1400–1600px wide, light theme, with the demo user (#1) that seeds on start.
A short GIF (`demo.gif`) of logging a meal → dashboard updating is even better.
